name: "Labels: PR"

on:
- pull_request_target

jobs:
  labeler:
    if: github.actor != 'Erida-change-bot'
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v6.1.0
      with:
        sync-labels: false
